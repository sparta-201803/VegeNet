<% if product.errors.any? %>
  <div class="alert alert-danger">
    <p><b><%= product.errors.count %>件のエラーがあります</b></p>

    <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
