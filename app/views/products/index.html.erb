<% @products.each do |product| %>

    <div class="col-md-3 col-sm-6 red">
      <div class="form-group">
        <%= image_tag product.image_url %><br>
        <%= product.producer %><br>
        <%= link_to product.product_id, product %>
      </div>
    </div>

<% end %>
